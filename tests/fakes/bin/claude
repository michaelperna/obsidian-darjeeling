#!/usr/bin/env bash
# Fake Claude Code CLI for Darjeeling tests. See ../fake_agent.py.
here="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
FAKE_AGENT_FLAVOUR=claude exec python3 "$here/../fake_agent.py" "$@"
